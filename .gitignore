.venv/
__pycache__/
*.py[cod]
*$py.class
.DS_Store
.cache/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz

# Logs & temp files
*.log
*.tmp
*.bak

# Cache and raw large feature vector dumps (>100MB)
phase_hs_results/*_hidden_states.csv*
# Raw feature dumps: too large for git in general. Six files under this path are
# tracked anyway (four layers.parquet, two generations.csv) because the paper's
# reproducibility statement points at them; they were added with `git add -f` and
# stay tracked despite this rule. Do not `git rm --cached` this directory.
sinktda_out/
hf_cache/
.hf_cache/
scratch/

*.old150

# editor/agent backups
*.bak2
*.old150

# Third-party reading material
refrences/
references/


# Working notes and review material: never ship these. They carry absolute paths,
# an earlier review's scores, and third-party PDFs, and the anonymous mirror serves
# whatever is in the repo.
HANDOFF.md
PLAN_v2.md
PROMPT_5080.md
PROMPT_REVITALIZE.md
REVIEW_AC.md
REVITALIZE_PLAN.md
rigor/patch_device.py
